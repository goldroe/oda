
tokens:

grouping:
( ) [ ] { }

unary:
+ - ! ~ & *

binary:
+ - | ^ << >>
* / % &
== != < <= >=
&&
||
? :

assign:
= := += -= |= ^= <<= >>= *= /= %=

literals:
IDENT  = [a-zA-Z][a-zA-z0-9_]*
FLOAT  = [0-9]*[.][0-9]+([eE][+-]?[0-9]+)?
INT    = [1-9][0-9]* | 0[xX][0-9a-fA-f]+ | 0[b][0-1]+ | 0[0-7]+
CHAR   = '\'' . '\''
STRING = '"' [^"]* '"'
